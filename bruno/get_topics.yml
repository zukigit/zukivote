info:
  name: get_topics
  type: http
  seq: 5

http:
  method: GET
  url: 127.0.0.1:8080/topics
  headers:
    - name: Authorization
      value: Bearer {{LOGIN_TOKEN}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
