info:
  name: get_photo
  type: http
  seq: 7

http:
  method: GET
  url: 127.0.0.1:8080/photo?item_id={{ITEM_ID}}
  headers:
    - name: Authorization
      value: Bearer {{LOGIN_TOKEN}}
  params:
    - name: item_id
      value: "{{ITEM_ID}}"
      type: query

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
