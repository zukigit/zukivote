info:
  name: get_voting
  type: http
  seq: 12

http:
  method: GET
  url: 127.0.0.1:8080/voting?topic_id={{TOPIC_ID}}
  headers:
    - name: Authorization
      value: Bearer {{LOGIN_TOKEN}}
  params:
    - name: topic_id
      value: "{{TOPIC_ID}}"
      type: query

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
